---
phase: 01-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - Cargo.toml
  - src/main.rs
  - src/cli.rs
  - src/commands/mod.rs
  - src/commands/summary.rs
  - src/commands/scan.rs
  - src/commands/largest.rs
  - src/commands/categories.rs
  - src/commands/hidden.rs
  - src/commands/caches.rs
  - src/commands/clean.rs
  - src/commands/config_cmd.rs
  - src/commands/doctor.rs
  - src/config/mod.rs
  - src/config/schema.rs
  - src/output/mod.rs
  - src/platform/mod.rs
  - src/platform/macos.rs
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
  - FOUND-05
  - FOUND-06

must_haves:
  truths:
    - "cargo build succeeds from the project root with zero errors"
    - "cargo test compiles and all tests pass (even if tests are stubs/assertions)"
    - "The directory tree src/commands/, src/config/, src/output/, src/platform/ all exist"
    - "Cargo.toml contains every required dependency at the correct version"
  artifacts:
    - path: "Cargo.toml"
      provides: "Project manifest with all Phase 1-8 dependencies declared"
      contains: "clap = { version = \"4.6\""
    - path: "src/main.rs"
      provides: "Binary entry point that compiles"
      contains: "fn main"
    - path: "src/cli.rs"
      provides: "Cli struct and Commands enum stubs"
      contains: "pub struct Cli"
    - path: "src/platform/macos.rs"
      provides: "Protected-path module behind cfg gate"
      contains: "#[cfg(target_os = \"macos\")]"
    - path: "src/config/schema.rs"
      provides: "Config struct with serde derives"
      contains: "pub struct Config"
    - path: "src/output/mod.rs"
      provides: "OutputFormat enum and write_json stub"
      contains: "pub enum OutputFormat"
  key_links:
    - from: "src/main.rs"
      to: "src/cli.rs"
      via: "mod cli; use cli::Cli;"
      pattern: "mod cli"
    - from: "src/main.rs"
      to: "src/commands/mod.rs"
      via: "mod commands;"
      pattern: "mod commands"
---

<objective>
Create the Freespace Rust project from scratch: run `cargo new`, write Cargo.toml with all dependencies, and create all source file stubs so the project compiles and `cargo test` passes.

Purpose: Establish the compilable skeleton that Wave 1 fills with real logic. Every module path, every file, and every test hook must exist before Wave 1 runs — Wave 1 must never create files, only fill them in.

Output: A compilable Rust binary with stub implementations in every command handler and inline test modules with placeholder tests (all passing).
</objective>

<execution_context>
@/Users/giorgos/Workspace/AI-Safe/GSD-Projects/.claude/get-shit-done/workflows/execute-plan.md
@/Users/giorgos/Workspace/AI-Safe/GSD-Projects/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/.planning/PROJECT.md
@/Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/.planning/ROADMAP.md
@/Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/.planning/phases/01-foundation/01-CONTEXT.md
@/Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project with cargo new and write Cargo.toml</name>
  <read_first>
    - /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/.planning/phases/01-foundation/01-RESEARCH.md (Standard Stack section — exact versions)
  </read_first>
  <files>Cargo.toml</files>
  <action>
Run from /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/:

```
cargo new freespace --name freespace
```

This creates src/main.rs and a minimal Cargo.toml. Then REPLACE the entire Cargo.toml with:

```toml
[package]
name = "freespace"
version = "0.1.0"
edition = "2021"
description = "macOS disk inspection and cleanup utility"
authors = []

[dependencies]
clap = { version = "4.6", features = ["derive"] }
walkdir = "2.5"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "1.1"
thiserror = "2.0"
anyhow = "1.0"
dirs = "6.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
trash = "5.2"
comfy-table = "7.2"
indicatif = "0.18"
owo-colors = "4.3"
sysinfo = "0.38"
rayon = "1.11"
bytesize = "2.3"

[dev-dependencies]
tempfile = "3.27"
assert_cmd = "2.0"
```

NOTE: Do not add `nix` yet — it requires feature flags and is only needed in Phase 2. Do not add `walkdir` to dev-dependencies; it is a regular dependency. The full stack (walkdir, trash, comfy-table, etc.) goes into [dependencies] now so later phases can use it without modifying Cargo.toml.

After writing Cargo.toml, run `cargo fetch` to verify all versions resolve successfully. If any version fails to resolve, adjust to the nearest available patch version and document the change.
  </action>
  <verify>
    <automated>cd /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace && cargo fetch 2>&1 | tail -5</automated>
  </verify>
  <done>cargo fetch completes without errors. Cargo.toml contains all listed dependencies with the exact versions shown above.</done>
</task>

<task type="auto">
  <name>Task 2: Create all source file stubs</name>
  <read_first>
    - /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/.planning/phases/01-foundation/01-RESEARCH.md (Architecture Patterns section — complete project structure)
  </read_first>
  <files>
    src/main.rs,
    src/cli.rs,
    src/commands/mod.rs,
    src/commands/summary.rs,
    src/commands/scan.rs,
    src/commands/largest.rs,
    src/commands/categories.rs,
    src/commands/hidden.rs,
    src/commands/caches.rs,
    src/commands/clean.rs,
    src/commands/config_cmd.rs,
    src/commands/doctor.rs,
    src/config/mod.rs,
    src/config/schema.rs,
    src/output/mod.rs,
    src/platform/mod.rs,
    src/platform/macos.rs
  </files>
  <action>
Working directory: /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace/

Create the following directory structure:
```
src/
  commands/
  config/
  output/
  platform/
```

Write each file exactly as specified below. These are STUBS — they must compile but do not need real logic (Wave 1 fills in the logic). Every stub must use the correct function signatures so Wave 1 can fill in the body without changing signatures.

--- src/main.rs ---
```rust
mod cli;
mod commands;
mod config;
mod output;
mod platform;

use anyhow::Context;
use clap::Parser;
use cli::{CleanCommands, Commands};

fn main() -> anyhow::Result<()> {
    init_logging();
    let cli = cli::Cli::parse();
    let config = config::load_config().context("Failed to load configuration")?;
    #[cfg(target_os = "macos")]
    let _protected = platform::macos::protected_paths();
    match cli.command {
        Commands::Summary => commands::summary::run(&config, cli.json),
        Commands::Scan { path } => commands::scan::run(&path, &config, cli.json),
        Commands::Largest { path } => commands::largest::run(&path, &config, cli.json),
        Commands::Categories { path } => commands::categories::run(&path, &config, cli.json),
        Commands::Hidden { path } => commands::hidden::run(&path, &config, cli.json),
        Commands::Caches => commands::caches::run(&config, cli.json),
        Commands::Clean { command } => match command {
            CleanCommands::Preview => commands::clean::run_preview(&config, cli.json),
            CleanCommands::Apply { force } => commands::clean::run_apply(force, &config, cli.json),
        },
        Commands::Config => commands::config_cmd::run(&config, cli.json),
        Commands::Doctor => commands::doctor::run(&config, cli.json),
    }
}

fn init_logging() {
    use tracing_subscriber::{fmt, EnvFilter};
    fmt()
        .with_writer(std::io::stderr)
        .with_env_filter(
            EnvFilter::try_from_default_env()
                .unwrap_or_else(|_| EnvFilter::new("warn")),
        )
        .with_target(false)
        .init();
}
```

--- src/cli.rs ---
```rust
use clap::{Parser, Subcommand};

#[derive(Parser)]
#[command(
    name = "freespace",
    about = "macOS disk inspection and cleanup utility",
    version,
    author
)]
pub struct Cli {
    /// Output results as JSON (stdout only; errors remain on stderr)
    #[arg(long, global = true)]
    pub json: bool,

    #[command(subcommand)]
    pub command: Commands,
}

#[derive(Subcommand)]
pub enum Commands {
    /// Show disk usage summary for all mounted volumes
    Summary,
    /// Scan a path and report size breakdown
    Scan {
        path: std::path::PathBuf,
    },
    /// Show largest files and directories at a path
    Largest {
        path: std::path::PathBuf,
    },
    /// Group disk usage into semantic categories at a path
    Categories {
        path: std::path::PathBuf,
    },
    /// List hidden files and directories at a path
    Hidden {
        path: std::path::PathBuf,
    },
    /// Discover cache directories across standard macOS locations
    Caches,
    /// Cleanup operations (preview or apply)
    Clean {
        #[command(subcommand)]
        command: CleanCommands,
    },
    /// Show and edit Freespace configuration
    Config,
    /// Run self-diagnostics
    Doctor,
}

#[derive(Subcommand)]
pub enum CleanCommands {
    /// Show files that would be removed (read-only, no changes made)
    Preview,
    /// Execute cleanup (moves to Trash by default)
    Apply {
        /// Permanently delete instead of moving to Trash
        #[arg(long)]
        force: bool,
    },
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn all_subcommands_compile() {
        // Verifies the CLI struct compiles with all required subcommands.
        // Full --help output tests are in Wave 1.
        let _ = std::mem::size_of::<Cli>();
        let _ = std::mem::size_of::<Commands>();
        let _ = std::mem::size_of::<CleanCommands>();
    }
}
```

--- src/commands/mod.rs ---
```rust
pub mod caches;
pub mod categories;
pub mod clean;
pub mod config_cmd;
pub mod doctor;
pub mod hidden;
pub mod largest;
pub mod scan;
pub mod summary;
```

--- src/commands/summary.rs ---
```rust
use crate::config::schema::Config;

pub fn run(config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (config, json);
    eprintln!("summary: not yet implemented");
    Ok(())
}
```

--- src/commands/scan.rs ---
```rust
use crate::config::schema::Config;
use std::path::Path;

pub fn run(path: &Path, config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (path, config, json);
    eprintln!("scan: not yet implemented");
    Ok(())
}
```

--- src/commands/largest.rs ---
```rust
use crate::config::schema::Config;
use std::path::Path;

pub fn run(path: &Path, config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (path, config, json);
    eprintln!("largest: not yet implemented");
    Ok(())
}
```

--- src/commands/categories.rs ---
```rust
use crate::config::schema::Config;
use std::path::Path;

pub fn run(path: &Path, config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (path, config, json);
    eprintln!("categories: not yet implemented");
    Ok(())
}
```

--- src/commands/hidden.rs ---
```rust
use crate::config::schema::Config;
use std::path::Path;

pub fn run(path: &Path, config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (path, config, json);
    eprintln!("hidden: not yet implemented");
    Ok(())
}
```

--- src/commands/caches.rs ---
```rust
use crate::config::schema::Config;

pub fn run(config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (config, json);
    eprintln!("caches: not yet implemented");
    Ok(())
}
```

--- src/commands/clean.rs ---
```rust
use crate::config::schema::Config;

pub fn run_preview(config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (config, json);
    eprintln!("clean preview: not yet implemented");
    Ok(())
}

pub fn run_apply(force: bool, config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (force, config, json);
    eprintln!("clean apply: not yet implemented");
    Ok(())
}
```

--- src/commands/config_cmd.rs ---
NOTE: This file is intentionally named config_cmd.rs (not config.rs) to avoid a name collision with the `config` module at the crate root.

```rust
use crate::config::schema::Config;

pub fn run(config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (config, json);
    eprintln!("config: not yet implemented");
    Ok(())
}
```

--- src/commands/doctor.rs ---
```rust
use crate::config::schema::Config;

pub fn run(config: &Config, json: bool) -> anyhow::Result<()> {
    let _ = (config, json);
    eprintln!("doctor: not yet implemented");
    Ok(())
}
```

--- src/config/schema.rs ---
```rust
use serde::{Deserialize, Serialize};

#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct Config {
    #[serde(default)]
    pub scan: ScanConfig,
    #[serde(default)]
    pub cleanup: CleanupConfig,
}

#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct ScanConfig {
    #[serde(default)]
    pub exclude: Vec<String>,
}

#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct CleanupConfig {
    #[serde(default)]
    pub safe_categories: Vec<String>,
}
```

--- src/config/mod.rs ---
```rust
pub mod schema;

pub fn load_config() -> anyhow::Result<schema::Config> {
    // Stub — real implementation in Wave 1 (Plan 02)
    Ok(Default::default())
}

#[cfg(test)]
mod tests {
    #[test]
    fn stub_returns_default() {
        let cfg = super::load_config().expect("stub must not fail");
        assert!(cfg.scan.exclude.is_empty());
    }
}
```

--- src/output/mod.rs ---
```rust
use std::io::{self, Write};

pub enum OutputFormat {
    Table,
    Json,
}

impl OutputFormat {
    pub fn from_flag(json: bool) -> Self {
        if json {
            OutputFormat::Json
        } else {
            OutputFormat::Table
        }
    }
}

pub fn write_json<T: serde::Serialize>(value: &T) -> anyhow::Result<()> {
    let stdout = io::stdout();
    serde_json::to_writer(stdout.lock(), value)?;
    writeln!(io::stdout().lock())?;
    Ok(())
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn from_flag_json() {
        assert!(matches!(OutputFormat::from_flag(true), OutputFormat::Json));
    }

    #[test]
    fn from_flag_table() {
        assert!(matches!(OutputFormat::from_flag(false), OutputFormat::Table));
    }
}
```

--- src/platform/mod.rs ---
```rust
#[cfg(target_os = "macos")]
pub mod macos;
```

--- src/platform/macos.rs ---
```rust
use std::path::PathBuf;

/// Returns the five protected root paths, canonicalized at startup.
/// Paths that cannot be canonicalized (non-standard environments or tests)
/// are stored as-is with a warning emitted to stderr.
#[cfg(target_os = "macos")]
pub fn protected_paths() -> Vec<PathBuf> {
    const RAW: &[&str] = &["/System", "/usr", "/bin", "/sbin", "/private"];
    RAW.iter()
        .map(|raw| {
            std::fs::canonicalize(raw).unwrap_or_else(|e| {
                tracing::warn!("Could not canonicalize protected path {raw}: {e}");
                PathBuf::from(raw)
            })
        })
        .collect()
}

/// Returns true if the given path starts with any protected path prefix.
#[cfg(target_os = "macos")]
pub fn is_protected(path: &std::path::Path, protected: &[PathBuf]) -> bool {
    protected.iter().any(|p| path.starts_with(p))
}

#[cfg(test)]
#[cfg(target_os = "macos")]
mod tests {
    use super::*;

    #[test]
    fn protected_paths_returns_five() {
        let paths = protected_paths();
        assert_eq!(paths.len(), 5);
    }

    #[test]
    fn is_protected_system() {
        let protected = vec![
            PathBuf::from("/System"),
            PathBuf::from("/usr"),
            PathBuf::from("/bin"),
            PathBuf::from("/sbin"),
            PathBuf::from("/private"),
        ];
        assert!(is_protected(
            std::path::Path::new("/System/Library/CoreServices"),
            &protected
        ));
    }

    #[test]
    fn is_protected_home_is_not_protected() {
        let protected = vec![
            PathBuf::from("/System"),
            PathBuf::from("/usr"),
            PathBuf::from("/bin"),
            PathBuf::from("/sbin"),
            PathBuf::from("/private"),
        ];
        assert!(!is_protected(
            std::path::Path::new("/Users/alice/Documents"),
            &protected
        ));
    }
}
```
  </action>
  <verify>
    <automated>cd /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace && cargo build 2>&1 | tail -10</automated>
  </verify>
  <done>
    cargo build completes with zero errors. All files listed in the task exist at their expected paths. The following tree should exist:
    src/commands/ (9 files: mod.rs + one per command)
    src/config/ (2 files: mod.rs + schema.rs)
    src/output/ (1 file: mod.rs)
    src/platform/ (2 files: mod.rs + macos.rs)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify all stub tests pass</name>
  <read_first>
    - /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace/src/cli.rs
    - /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace/src/config/mod.rs
    - /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace/src/output/mod.rs
    - /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace/src/platform/macos.rs
  </read_first>
  <files>
    src/cli.rs,
    src/config/mod.rs,
    src/output/mod.rs,
    src/platform/macos.rs
  </files>
  <action>
Run `cargo test` from /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace/.

Expected passing tests:
- cli::tests::all_subcommands_compile
- config::tests::stub_returns_default
- output::tests::from_flag_json
- output::tests::from_flag_table
- platform::macos::tests::protected_paths_returns_five
- platform::macos::tests::is_protected_system
- platform::macos::tests::is_protected_home_is_not_protected

If any test fails, read the error carefully. Common failures and fixes:
- "cannot find module" — verify the file exists and mod.rs declares it correctly
- "cfg gate" — macos tests only run on macOS; if running on Linux in CI, these tests are skipped by the cfg gate (not a failure)
- "mismatched types" — verify function signatures in stub files match what main.rs expects

Fix any compilation or test errors before marking this task done. Do not move to Wave 1 until `cargo test` exits with code 0.
  </action>
  <verify>
    <automated>cd /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace && cargo test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test` exits with code 0
    - Output contains "test result: ok"
    - No compiler warnings about unused imports that would become errors under `#[deny(warnings)]`
    - `cargo run -- --help` prints a help message listing at least: summary, scan, largest, categories, hidden, caches, clean, config, doctor
    - `cargo run -- clean --help` lists: preview, apply
  </acceptance_criteria>
  <done>cargo test passes. cargo run -- --help lists all subcommands. cargo run -- clean --help lists preview and apply.</done>
</task>

</tasks>

<verification>
After all three tasks:

```bash
cd /Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/freespace
cargo test
cargo run -- --help 2>&1 | grep -E "summary|scan|largest|categories|hidden|caches|clean|config|doctor"
cargo run -- clean --help 2>&1 | grep -E "preview|apply"
```

All commands must succeed. The help output must contain all required subcommand names.
</verification>

<success_criteria>
- `cargo build` succeeds with zero errors
- `cargo test` passes all tests (green)
- `cargo run -- --help` lists: summary, scan, largest, categories, hidden, caches, clean, config, doctor
- `cargo run -- clean --help` lists: preview, apply
- All 17 source files exist at their declared paths
- Cargo.toml contains clap 4.6, serde 1.0, thiserror 2.0, anyhow 1.0, dirs 6.0, tracing 0.1, tracing-subscriber 0.3
</success_criteria>

<output>
After completion, create `/Users/giorgos/Workspace/AI-Safe/GSD-Projects/Freespace/.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
